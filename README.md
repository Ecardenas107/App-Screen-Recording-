# App-Screen-Recording-
This Python application records the computer screen while overlaying webcam footage. It uses OpenCV, PIL, and NumPy to capture and process video frames. The recording is saved as an MP4 file with a timestamp, allowing users to create screen recordings with live camera input.
